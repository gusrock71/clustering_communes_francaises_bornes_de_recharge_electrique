requests>=2.31
boto3>=1.34
duckdb>=1.0
pyspark>=3.5
python-dotenv>=1.0
pandas>=2.0,<3.0
pyarrow>=18.0
sqlalchemy>=2.0
psycopg2-binary>=2.9  # driver Postgres (Neon/Supabase) ; SQLite (stdlib) suffit pour les tests
dbt-core>=1.8,<2.0    # transformation (dbt/), remplace la jointure/cleaning/feature engineering DuckDB
dbt-postgres>=1.8,<2.0  # adaptateur Postgres/Neon utilisé par dbt/profiles.yml
scikit-learn>=1.4     # K-Means + StandardScaler (ve_pipeline/clustering/)
mlflow>=2.14          # tracking des runs K-Means (params/metrics/model)
